# Shared UI

## Purpose
Reserves space for UI concerns shared across features without collapsing everything into a generic utility bucket.

## Contents
- Directory-level conventions for shared UI code.

## Dependency Rules
- Move code here only when at least two features need it.

## Change Notes
- Keep this directory empty until a second feature creates a real shared concern.

