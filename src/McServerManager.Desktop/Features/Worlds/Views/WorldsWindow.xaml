<Window
    x:Class="McServerManager.Desktop.Features.Worlds.Views.WorldsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="McServerManager">
    <Grid Padding="20" RowSpacing="16" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.RowSpan="2" Padding="16" CornerRadius="12" Background="#1B2330">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Content="Refresh" Command="{Binding RefreshCommand}" />

                <TextBox Grid.Row="1"
                         Header="New world name"
                         Text="{Binding NewWorldName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         PlaceholderText="Weekend Vanilla" />

                <Button Grid.Row="2" Content="Create Draft" Command="{Binding CreateDraftCommand}" />

                <ListView Grid.Row="3"
                          ItemsSource="{Binding Worlds}"
                          SelectedItem="{Binding SelectedWorldItem, Mode=TwoWay}"
                          SelectionChanged="OnWorldSelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Spacing="2">
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding StatusLabel}" Opacity="0.72" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <Border Grid.Column="1" Grid.Row="0" Padding="16" CornerRadius="12" Background="#F5F0E6">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Header="Display name"
                             Text="{Binding CurrentDisplayName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="1" Margin="12,24,0,0" Content="Save" Command="{Binding SaveCommand}" />
                    <Button Grid.Column="2" Margin="12,24,0,0" Content="Activate" Command="{Binding ActivateCommand}" />
                    <Button Grid.Column="3" Margin="12,24,0,0" Content="Revert" Command="{Binding RevertCommand}" />
                    <Button Grid.Column="4" Margin="12,24,0,0" Content="Delete" Command="{Binding DeleteCommand}" />
                </Grid>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16">
                    <TextBlock Text="{Binding CurrentStatusLabel}" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding CurrentSlugLabel}" />
                    <TextBlock Text="{Binding DirtyLabel}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Header="server.properties"
                     Text="{Binding ServerPropertiesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     FontFamily="Cascadia Mono"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch" />

            <TextBox Grid.Column="1"
                     Header="whitelist.json"
                     Text="{Binding WhitelistJsonText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     FontFamily="Cascadia Mono"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch" />
        </Grid>

        <Border Grid.Column="1" Grid.Row="2" Padding="12" CornerRadius="12" Background="#E0E8EF">
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="WrapWholeWords" />
        </Border>
    </Grid>
</Window>

